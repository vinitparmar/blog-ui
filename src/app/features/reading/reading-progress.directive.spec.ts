import { ReadingProgressDirective } from './reading-progress.directive';

describe('ReadingProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadingProgressDirective();
    expect(directive).toBeTruthy();
  });
});
